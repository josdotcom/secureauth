spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5433/secureauth?options=-c%20TimeZone%3DUTC
    username: secureauth
    password: secureauth
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true
    show-sql: true
server:
  port: 8085